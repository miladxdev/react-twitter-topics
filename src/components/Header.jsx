function Header({title}) {
    return (
        <div className="wrap">
            <div className="container">
                <h2>{title}</h2>
            </div>
        </div>
    );
}

export default Header;
